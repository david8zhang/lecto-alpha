<html>
	<head>
		<!-- Boostrap components -->
        <link href="https://bootswatch.com/flatly/bootstrap.min.css" rel="stylesheet">
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>	

		<nav class="navbar navbar-default navbar-fixed-top">
		  <div class="container-fluid">
		   		<a href="#" class="navbar-brand">Classes</a>
		   </div>
		</nav>
	</head>

	<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.6.4/jquery.min.js"></script>
	<script src="/socket.io/socket.io.js"></script>  

	<script>	
		// //on connection to server, ask for user's name 
		// var socket = io.connect('http://localhost:5400');
		// socket.on('connect', function(){
		// 	//call the serverside function, add user and send the value of the prompt
		// 	socket.emit('adduser', prompt("What's your name, friend?"));
		// });

		// socket.on('updatechat', function (username, data){
		// 	//udate the chat body here
		// })

		// //listener, whenever server emits 'updaterooms', this updates the room the client is in
		// socket.on('updaterooms', function(room, current_room){
		// 	// $('#rooms').empty();
		// 	// $.each(rooms, function(key, value) {
		// 	// 	if(value == current_room){
		// 	// 		$('#rooms').append('<div>' + value + '</div>');
		// 	// 	}
		// 	// 	else {
		// 	// 		$('#rooms').append('<div><a href="#" onclick="switchRoom(\''+value+'\')">' + value + '</a></div>');
		// 	// 	}
		// 	// });
		// })

		// function switchRoom(room){
		// 	socket.emit('switchRoom', room);
		// }

		
	</script> 

</html>